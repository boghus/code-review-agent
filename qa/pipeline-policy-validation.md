# Pipeline Policy QA Validation

This file exists only to exercise the `qa/*` execution context in CI.

The expected pipeline behavior is defined by the context-aware CI implementation in the parent branch.